- const { use, lazyload } = theme.comments
- const { appId, appKey, avatar, serverURLs, visitor, option } = theme.valine

- let emojiMaps = '""'
if site.data.valine
  - emojiMaps = JSON.stringify(site.data.valine)

script.
  (() => {
    const isMoments = GLOBAL_CONFIG_SITE.pageType === 'moments'
    const option = !{JSON.stringify(option)}

    const initValine = (el, path) => {
      if (isMoments) {
        window.momentsComment.destroyValine = () => {
          if (el.children.length) {
            el.innerHTML = ''
            el.classList.add('no-comment')
          }
        }
      }

      const valineConfig = {
        el: '#vcomment',
        appId: '#{appId}',
        appKey: '#{appKey}',
        avatar: '#{avatar}',
        serverURLs: '#{serverURLs}',
        emojiMaps: !{emojiMaps},
        visitor: #{visitor},
        ...option,
        path: isMoments ? path : (option && option.path) || window.location.pathname
      }

      new Valine(valineConfig)
    }

    const loadValine = async (el, path) => {
      if (typeof Valine === 'function') {
        initValine(el, path)
      } else {
        await btf.getScript('!{url_for(theme.asset.valine)}')
        initValine(el, path)
      }
    }

    if (isMoments) {
      '!{use[0]}' === 'Valine'
        ? window.momentsComment = { loadComment: loadValine }
        : window.loadOtherComment = loadValine
      return
    }

    if ('!{use[0]}' === 'Valine' || !!{lazyload}) {
      if (!{lazyload}) btf.loadComment(document.getElementById('vcomment'),loadValine)
      else setTimeout(loadValine, 0)
    } else {
      window.loadOtherComment = loadValine
    }
  })()